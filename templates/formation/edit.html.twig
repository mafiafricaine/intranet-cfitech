{% extends 'base.html.twig' %}

{% block title %}Modification Formation{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1>Modifier Formation</h1>

            {{ include('formation/_form.html.twig', {'submitButtonText': 'Modifier Formation'}) }}

            <a href="{{ path('app_formation_index') }}">Retour à la liste</a>

            {{ include('formation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
